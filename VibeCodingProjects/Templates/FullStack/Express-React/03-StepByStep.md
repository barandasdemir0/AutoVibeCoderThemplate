# 📝 Steps | 📂 Files | 🐛 Debug | 📚 Resources — MERN (03-06)
## Steps
1. [ ] Server: `npm init` → Express + Mongoose → models → controllers → routes
2. [ ] JWT auth middleware → login/register endpoints
3. [ ] Client: `npm create vite@latest -- --template react`
4. [ ] React: routing, pages, Axios service, auth flow
5. [ ] Redux/Zustand → state management
6. [ ] Docker Compose: mongo + server + client

## Files
```
server/src/ → config/, models/, routes/, controllers/, services/, middleware/, validators/, utils/, app.js
client/src/ → components/, pages/, services/, hooks/, store/, App.jsx
```

## Naming Kuralları
| Katman | Kural |
|--------|-------|
| JS Module | camelCase.js |
| Model | PascalCase (User, Product) |
| Route | kebab-case URL (/api/user-profiles) |
| React Component | PascalCase.jsx |
| MongoDB Collection | lowercase plural (users, products) |

## Debug Günlüğü
| Tarih | Hata | Çözüm |
|-------|------|--------|
| [—]   | [—]  | [—]    |
